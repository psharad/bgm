#double
#admin_content.grid_12
  #crop_area
    .view= link_to image_tag(@image.image.url(:large)), @image.image.url(:large), :id => 'image'
    .desc= @image.description
    #double
    = link_to 'Back', images_url, :class => 'black-button'